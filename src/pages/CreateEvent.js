import React from 'react';
import { Typography, Container } from '@mui/material';

const CreateEvent = () => {
  return (
    <Container>
      <Typography variant="h3">Create Event Page</Typography>
    </Container>
  );
};

export default CreateEvent;
