import React from 'react';
import { Typography, Container } from '@mui/material';

const AllEvents = () => {
  return (
    <Container>
      <Typography variant="h3">All Events Page</Typography>
    </Container>
  );
};

export default AllEvents;
