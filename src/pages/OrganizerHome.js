import React from 'react';
import { Typography, Container } from '@mui/material';

const OrganizerHome = () => {
  return (
    <Container>
      <Typography variant="h3">Organizer Home Page</Typography>
    </Container>
  );
};

export default OrganizerHome;
